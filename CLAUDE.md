# DevAgent - 임원 전용 에이전트 팀

삼성전자 DX사업부 AI Center 연구개발 담당 임원의 개인 에이전트 팀 저장소.
각 에이전트는 임원이 반복적으로 하던 판단·조사·정리 작업을 하나씩 대신한다.

## 디렉터리 규약

```
.claude/agents/<name>.md          # 에이전트 정의 (서브 에이전트 본체)
.claude/agents/TEAM.md            # 팀 로스터. 에이전트 추가/변경 시 반드시 갱신
.claude/skills/<name>/SKILL.md    # 수동 호출용 슬래시 커맨드 (/<name>)
<domain>/config/                  # 에이전트가 읽는 설정·감시 대상 정의
<domain>/state/                   # 실행 간 유지되는 상태 (중복 제거 원장 등)
<domain>/reports/                 # 날짜별 산출 리포트
<domain>/artifact/                # Artifact 로 발행하는 HTML
```

`<domain>` 은 에이전트별 작업 공간이다 (예: `monitor/`). 여러 에이전트가 상태를 섞어 쓰지 않는다.

## 팀 공통 원칙

1. **임원 산출물 기준** - 결론이 맨 앞. 5분 내 읽고 의사결정에 쓸 수 있어야 한다. 과정 설명은 뒤로.
2. **근거 없는 주장 금지** - 성능 수치는 원문 확인 후 인용. 초록/보도자료만 보고 옮기지 않는다.
3. **상태는 파괴하지 않는다** - `state/` 의 이력 파일은 절대 초기화하지 않는다. 같은 내용을 두 번
   보고하는 것이 가장 큰 실패다.
4. **자동 + 수동 양방향** - 정기 실행 에이전트는 항상 짝이 되는 `/<name>` 스킬을 함께 갖는다.
5. **도구는 최소 권한** - `tools:` 에 실제로 필요한 것만 나열한다.

## 에이전트 추가 방법

프롬프트를 붙여넣고 "에이전트화" 를 요청하면 `agentify` 스킬이 위 규약대로 변환·등록한다.
현재 로스터는 `.claude/agents/TEAM.md` 참조.
